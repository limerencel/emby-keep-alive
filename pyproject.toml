[project]
name = "emby-alive"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.12.14",
    "python-dateutil>=2.9.0.post0",
    "requests>=2.32.4",
]

[tool.uv.workspace]
members = ["emby-url-extractor"]
